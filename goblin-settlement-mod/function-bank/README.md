# 独立函数任务库

用途：把边界清楚、无需游戏状态的计算交给其他模型；交付代码经过验收后，在相应模块开发时接入。F001 主线已有已验证的独立实现；F002、F003 外部提交仍待主程序正式验收，F004、F005 新提交已到，均只做了静态初审。

## 使用方法

1. 从下表选择一张任务卡，把整个文件发给另一个模型即可；不需要发送完整游戏计划和聊天历史。
2. 让它交付 Java 源文件、独立验证文件和 NOTES.md。能访问本项目时写入对应 submissions 子目录；否则用户将输出保存到该目录。
3. 提交后状态仍是“待验收”。主开发者先检查规则、接口、边界、许可证/来源及验证内容；遵照用户当前“初版完成后统一测试”的约定，独立验证命令也后移。不能仅凭提交者说通过就接受。
4. 验收通过，将该修订的源文件、验证和记录复制到 accepted/任务编号/修订号，并登记验收证据。修订有变化就重新验收。
5. 开发对应模块时按登记表找到候选成果，接入运行时源码并验证游戏调用。登记接入路径。此后维护运行时那一份，accepted 中的旧副本只是历史快照，不是第二个需要同步维护的实现。

## 任务登记表

| 编号 | 任务卡 | 当前状态 | 交付位置 | 主线用途 |
| --- | --- | --- | --- | --- |
| F001 | [坐标转地块](tasks/F001-plot-coordinates.md) | 主线独立实现已验证并接入；外部 r1 仅静态审查，未正式验收 | submissions/F001/r1/ | 已接入地块与领地系统，外部版本不重复接入 |
| F002 | [材料缺口](tasks/F002-material-shortages.md) | r1 已收到并静态审查；统一测试前待验收 | submissions/F002/r1/ | 真实箱子库存与任务需求快照 |
| F003 | [道路单步代价](tasks/F003-road-step-cost.md) | r1 已收到并静态审查；统一测试前待验收 | submissions/F003/r1/ | 道路规划器的步代价 |
| F004 | [跨仓库取料方案](tasks/F004-warehouse-withdrawal-plan.md) | r1 已收到并静态审查；待统一测试与正式验收 | submissions/F004/r1/ | 多仓库实际扣料前的确定性方案 |
| F005 | [建筑地块排序](tasks/F005-building-plot-ranker.md) | r1 已收到并静态审查，检查程序一条反向断言已修正；待统一测试与正式验收 | submissions/F005/r1/ | 住房与工坊候选地块排序 |

状态依次为：待实现 → 待验收 → 需返工 / 已验收 → 已接入。登记验收时补充日期、修订号、运行命令与结果、最终文件路径；只有实际发生才改变状态。

F001 于 2026-09-24 由主线开发直接按规格 v1 实现；之后收到外部 r1 候选，但主线不重复接入，外部版本没有 accepted 快照。验证文件为 `src/test/java/dev/local/goblinsettlement/planning/math/PlotCoordinatesCheck.java`；本机 Gradle 9.2.1 的 `build --offline --no-daemon` 运行该检查通过。此结果只验证纯计算与工程编译，游戏中的地块权限另行验证。

已有 `src/main/java/dev/local/goblinsettlement/colony/PopulationRules.java`，不重复派发人口公式；人口相关运行时代码已接线，但新流程按当前约定尚未统一测试，不能据此宣称出生或存档已验证。

## 成本与接口约定

- 当前只追加 F004、F005 两张与主线边界清楚的任务卡，避免大量囤积；不把模型品牌当作质量保证，按结果验收。
- 一张卡应足够独立，又有实际复用价值。普通加减法直接使用 Java 运算，不为了分工另造一层函数。
- 先写清输入、输出、单位、异常、边界和例子，再安排实现；不要先大量囤积用途不清楚的函数。
- 计算只用 Java 21 标准库，不调用 Minecraft、Fabric、网络、文件、当前时间、随机数或全局可变状态，不修改输入。
- 卡片的函数签名是本次交付契约；数学行为验证后尽量保持稳定。未来适配 Minecraft 的坐标、物品和存档类型，在调用层转换。
- 需求真的改变时升级任务版本，并更新验收案例；不要随手修改含义后继续沿用旧验收结论。
- 函数验证通过只表示输入输出契约成立。区块是否活动、玩家是否允许施工、库存是否已被拿走等必须由游戏集成层重新判断。

## 不提前拆成普通小函数外包的部分

实际扣料与预留提交、出生名额登记、实体死亡与卸载、任务中断恢复、存档迁移、桥梁安全和世界修改权限，需要结合真实状态设计与验证。可以外包其中明确的纯计算片段，但主程序负责把整个过程正确地串起来。

本目录位于 src 之外，当前 Gradle 不会自动将 submissions/accepted 中的 Java 文件打进模组。
