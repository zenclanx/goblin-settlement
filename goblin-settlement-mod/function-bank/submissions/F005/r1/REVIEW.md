# F005 r1 静态审查记录

- 日期：2026-09-26（Asia/Shanghai）。
- 实现源码按规格 v1 的接口、输入边界、评分、稳定排序做了静态审查，未发现需返工的实现问题。
- 检查程序原本在“全 MAX 同分、逆序输入”处把预期值误写为 `allMaxRight`。主程序审查时改为 `allMaxLeft`，因为 X=0 的候选在 X=1 之前；这是检查程序修正，不涉及运行时实现。
- 遵照用户当前约定，未执行 javac、检查程序或游戏集成；此记录不是正式验收，不进入 accepted，也不接入运行时。统一测试时仍需独立运行并确认结果。
